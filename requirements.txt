# Direct dependencies for hard-math exploration.
# Install: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# See requirements.lock.txt for a full pinned transitive set from this machine.

# Computer algebra / numerics
sympy==1.14.0
numpy==2.5.2
scipy==1.18.0
mpmath==1.3.0
gmpy2==2.3.1
python-flint==0.9.0

# Data / plots / graphs
pandas==3.0.5
matplotlib==3.11.1
networkx==3.6.1

# Finite fields / coding / lattices-adjacent
galois==0.4.11

# Optimization / SAT / SMT
pulp==3.3.2
ortools==9.15.6755
cvxpy==1.9.2
z3-solver==5.1.0.0

# Notebooks / tests / utilities
jupyter==1.1.1
ipython==9.16.1
pytest==9.1.1
tqdm==4.70.0
joblib==1.5.3
