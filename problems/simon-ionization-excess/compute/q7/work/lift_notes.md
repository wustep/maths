# Lift — more bins at aspect 10

Same Newton $Q=I/D$ and mass-opt cut $Q>10/11$ as q6. The q6
bottleneck was the compact $\gamma$ at $R=10$, $n=32$
($\varphi=0.9108$, $P$ error $\approx 0.00381$), not the cut.

$R\le 9$ with the mass-opt cut cannot beat $1.1026$. Face
enumeration at $n>32$ is the live line: $10/11$ still has slack
at the q6 $\gamma$, and $P_{\max}$ shrinks as $q=10^{1/n}\to 1$.

The withdrawn $1.1168$ stays withdrawn. Finite $Z$ and $N_0(Z)-Z$
stay residue.

Replay: `../run_all.sh`.
