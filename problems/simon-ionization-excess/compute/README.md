# compute — simon-ionization-excess

`q1/` replays the published record and the remainder tightening.
`q2/` is the later search for a leading coefficient below $1.1185$
(residue).

```bash
./run_all.sh
```
