# q2 — small-Z residue

Published envelopes at integer $Z=2,3,4,5$, then attempts at
$N_c(2)<4$ or $N_c(2)<3$. Residue: nothing certifies a finite-$Z$
improvement of Lieb. Hydrogen uniqueness is not claimed.

```bash
./run_all.sh
```

Writes `certs/smallz.json`. Notes: `work/smallz_notes.md`.
