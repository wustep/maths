# Line F — homogeneous / quasi-homogeneous isolated cycles

Status: imagined, then work backwards.

Imagined certificate. A homogeneous planar field of degree n ≥ 2
has n isolated periodic orbits. Polar identities
ṙ = r^n f(θ), θ̇ = r^{n-1} g(θ) produce n simple zeros of f on
the circle.

Expected drop. If f ≡ 0 and g never vanishes, every circle is
periodic. If f has isolated zeros, those are invariant rays.
Zero isolated periodic orbits.

Fork to keep if the fiction dies. Unperturbed homogeneous fields,
and unperturbed quasi-homogeneous centers (weights (s1,s2)), have
no isolated periodic orbits. Exact polar / weighted-polar
identities in Z or Q, Python and a second language. Not a bound
on H(n).

Replay: `./run.sh`
