# R(5,5) / q1

Families left open after the 2026-08-17 algebraic census: one-edge-flip
extensions of the 656 published `(5,5,42)`-graphs; Cayley graphs on the
four groups of order 44 and the non-cyclic group of order 45; graphs
with a 7-cycle automorphism.

Replay:

```
./run_all.sh
```

Parent replay (McKay 656, circulant 42/43, flips, Seidel):

```
cd .. && ./replay.sh
```

A new `(5,5)`-graph on 43, 44, or 45 vertices would move the published
interval `43 ≤ R(5,5) ≤ 46`. An isolated SAT timeout does not.
