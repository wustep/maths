Small DRAT proofs for n=18, d=6 cubes that finished quickly.

Replay, from `compute/q1/` after `./build_solvers.sh`:

```
for f in certs/keep/ch-18-6-k{6,7,8,9,10,11}.drat; do
  cnf=${f%.drat}.cnf
  ./bin/drat-trim "$cnf" "$f" | tail -1
done
for f in certs/keep/ch-18-6-k0-t{1,2,3,4,5}.drat; do
  cnf=${f%.drat}.cnf
  ./bin/drat-trim "$cnf" "$f" | tail -1
done
```

Also stored: `ch-18-6-k1-t{1..5}.{cnf,drat}`.

Cubes k=2,3,4,5 are also UNSAT (kissat); their proofs were tens of megabytes and are regenerated by `solve.py --n 18 --d 6 --indeg0 k --proof`. The whole k=1 cube is UNSAT in 587s with a 1.207 GB DRAT that `drat-trim` accepted (sha256 in `ch-18-6-k1.sha256`). That file is not stored. k=0 is reduced to the t=6 slice.
