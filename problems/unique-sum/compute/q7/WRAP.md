# Cutoff record

No new bound. The campaign stopped during preparation, before any search
or certificate replay.

- Read the problem, finite-handle queue, attack history, and q5–q6 code.
- Opened OEIS A398173 in the browser: still 20 terms through 73, matching
  the inherited source snapshot. Selected 79 as the next unpublished row.
- Read Bedert's definition in the browser: unordered uniqueness forbids
  ordered multiplicities 1 and 2.
- The shell paper fetches and OEIS JSON fetch failed on sandbox DNS.
  The escalation call was interrupted at cutoff; no successful shell
  fetch result was returned. The two local paper text files were empty.
- No engine was modified or launched. No banked usage reset was used.

The first implementation dependency is a wider mask: q5 uses 64-bit
state, pair, affine-image, and memo keys in C and Rust. Changing only the
prime check would be incorrect. Controls across the 64-bit boundary and
independent witness checks would be required before trusting any result.
This work was not attempted before the stop.

The cutoff instruction superseded further hunting. This folder records
the unfinished handle so it cannot be mistaken for a completed exclusion.
